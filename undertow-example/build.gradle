group 'me.bayes'
version '1.0-SNAPSHOT'

apply plugin: 'java'

dependencies {
    compile project(':crashub-ext')
    compile spring_boot_undertow
}

jar {
    baseName = 'undertow-example'
    version =  '1.0.0'
}

jar.doLast { task ->
    ant.checksum file: task.archivePath
}
